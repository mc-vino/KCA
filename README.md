# cashforensics

Форензик-анализ операционных касс пунктов выдачи заказов.

На вход — выгрузка из 1С «Карточка счёта 50.2» по одному ПВЗ. В том же листе
справа лежат операционные логи фронтальной системы: блоки РКО (расходные
ордера) и ПКО (приходные ордера). Это два независимых потока данных, строки
которых **не** выровнены между собой.

Приложение отвечает на один главный вопрос: **из-за каких конкретно документов
сальдо кассы на конец периода отличается от целевого** — и раскладывает
отклонение по причинам так, чтобы раскладка сходилась без остатка.

Реализация по `ТЗ_кассовый_форензик_анализатор.md` (версия 1.0).

> **Статус: этапы 1–4 из четырёх (§14 ТЗ) реализованы.** Все двенадцать стадий
> конвейера §5 работают: `ingest → normalize → validate → classify →
> neutralize_reversals → reconcile → collapse_timing → balance_trace →
> localize → decompose → rank → report`. Есть сигнатуры §8.1–8.2,
> материальность §7.5, confidence §7.6, отчёты §9 (Excel, JSON, HTML) и
> команды `analyze`, `batch`, `verify`, `explain`.
>
> **Калибровка §11.3 пройдена полностью.** Все девять касс таблицы §11.3
> прогнаны: сальдо совпадает с эталоном, раскладка сходится
> (`|unresolved| < EPS_TIE`), V1–V3 проходят. Критерии §13.1 и §13.2 закрыты.
>
> | Касса | Сальдо | Эталон §11.3 | Остаток |
> | --- | --- | --- | --- |
> | `PAX_119011650` Солигорск | −1 224,80 | −1 224,80 | 0,00 |
> | `PAX_119049688` Витебск | −160,89 | −160,89 | 0,00 |
> | `Максиму_3` | +448,27 | +448,27 | 0,00 |
> | `горновых` | −253,80 | −253,80 | 0,00 |
> | `Щучин` | −4 952,36 | −4 952,36 | 0,00 |
> | `Максиму_касса_2` | +2 617,79 | +2 617,79 | 0,00 |
> | `Кса_с_отклонением` | −708,01 | −708,01 | 0,00 |
> | `PAX_119023531` | −293,62 | −293,62 | 0,00 |
> | `Кса_норма` | +10 632,25 | +10 632,25 | 0,00 |
>
> Именованные ожидания §11.3 воспроизведены дословно: `горновых` — два сторно
> 186,18 и 1 505,05, возвраты +299,80 на пяти датах, приход −70 округление,
> инкассация 105 → 0; `Максиму_3` — сторно-пара **R814←R877** (2 478,09), приход
> +439,33 на 10.02, пять дней округления на −119,00, инкассация 59 → 0 (lag 1),
> возвраты 15 → 11; `Кса_с_отклонением` — двойное списание возврата 602,85 и
> инкассация +4 038,06 как краевой артефакт; `Щучин` — инкассация −8 301,10
> таймингом и признак среза периода.
>
> **`PAX_119023531` — критерий готовности этапа 3 §14** («причинная раскладка
> воспроизводится строка в строку»). Раскладка состоит ровно из одной названной
> строки — **R811 · РКО 00284721 · 2 579,00** — и остатка. Совпали и все
> остальные величины: вариант C без колонки «Кому выдано», time-fallback принят
> на **357/368** днях, задвоенный ПКО 1 400,00 на строках **R87/R101**, cutoff
> 772,71, первый минус 09.08.2023, пол кассы +50,00 → −1 129,00.
>
> **Причинная раскладка §5.10.** ТЗ приводит эталон для `PAX_119023531` прямо в
> тексте: четыре строки, суммирующиеся в −293,62. Совпали две из них точно —
> **−2 579,00** (R811 · РКО 00284721) и **+1 400,00** (R87/R101, задвоенный
> ПКО). Витебск даёт эталонную раскладку целиком: одна строка −160,89 и нулевой
> остаток.
>
> Третья строка эталона, **+772,71** «инкассация не проведена», теперь тоже
> совпадает точно — она приходит отказом §5.6 по режиму встречных потоков.
> Четвёртая (+112,67 «размен + округления») у нас показана подробнее: прочие
> потоки +245,38, дисбаланс логов −150,00, семь дней округления прихода
> −394,85 и остаток +412,14 — в сумме те же 112,67.
>
> Раскладка выводится **тремя блоками** и с колонкой «уровень». Стороны §5.6
> делают её читаемой на кассах со встречными потоками: плоским списком
> Солигорск выглядел так, будто отклонение вдевятеро больше, чем оно есть.
> Уровень отвечает на второй вопрос — чем строка доказана. §7.4 запрещает
> спускаться ниже дневного агрегата продаж, но день, категорию и величину при
> этом знает точно, и раньше всё это молча падало в «не локализовано»: у
> `Максиму_3` и `Щучина` одна строка покрывала **всё** отклонение.
>
> | Касса | до документа | до дня и категории | структурное §5.10 | не названо | = отклонение |
> | --- | ---: | ---: | ---: | ---: | ---: |
> | Солигорск | −8 052,80 | −547,56 | −10 636,65 | +18 012,21 | −1 224,80 |
> | Витебск | −160,89 | 0,00 | 0,00 | 0,00 | −160,89 |
> | PAX_119023531 | −1 179,00 | +377,86 | +95,38 | +412,14 | −293,62 |
> | Кса_с_отклонением | −870,82 | −4 038,06 | +863,14 | +3 337,73 | −708,01 |
> | горновых | −1 347,00 | −70,00 | +70,00 | +1 093,20 | −253,80 |
> | Максиму_касса_2 | −74 462,99 | 0,00 | +439,85 | +76 640,93 | +2 617,79 |
> | Кса_норма | −16 888,05 | 0,00 | 0,00 | +27 520,30 | +10 632,25 |
> | Максиму_3 | 0,00 | +329,27 | +119,00 | **0,00** | +448,27 |
> | Щучин | 0,00 | −7 449,59 | +360,00 | +2 137,23 | −4 952,36 |
>
> `Максиму_3` разложена **без остатка** и строка в строку по §11.3: приход
> +439,33 на 10.02 (агрегат, Z-отчёт) и пять дней округления −119,00, которым
> отвечают ровно те же пять ПКО по 76.9.1 в «прочих потоках» (+119,00). Это та
> самая пара, про которую §5.4 говорит «объяснять отдельной строкой, а не
> прятать»: в опер-логе такой ПКО лежит в блоке прихода, а в приход 1С по §5.4
> не входит. У `Щучина` названы обе половины эталона — остановка прихода
> 07–12.05.2026 на −15 440,69 и тайминг инкассации +8 301,10, — а в остатке
> ровно то, что §11.3 называет «возвраты −2 137 реальные».
>
> Структурный блок — слагаемые самого тождества §5.10 (прочие потоки, дисбаланс
> логов). Расхождением учёта они не являются, и в стороны §5.6 не входят, но и
> прятать их нельзя: без них раскладка не сходится. На кассах варианта D (нет
> блока ПКО) структурная строка не выводится вовсе — там «дисбаланс логов»
> вырождается в минус всю сумму РКО, и показывать −632 848,10 при отклонении
> +10 632,25 значило бы выдавать артефакт выгрузки за измерение. Такие кассы
> получают остаток с названной причиной: «блока опер-лога для ПРИХОДА нет».
>
> Задвоенный ПКО теперь несёт базовую частоту §8.1 — без неё находка читалась
> как утверждение. Нуль-модель: те же суммы, те же дни, случайная приписка сумм
> к проводкам внутри счёта; перестановки берут `seed` и `PERMUTATION_B` из
> конфига. На Солигорске счёт 62.4.1 даёт 971 проводку на 789 дней, и две
> наблюдённые пары (+1 822,12 и +24,87) объясняются случаем: ожидается 0,60,
> p = 0,12. На `PAX_119023531` тот же счёт — 81 проводка на 78 дней, ожидается
> 0,05 при одной наблюдённой, p = 0,05. Отсюда и разница в эталоне: §11.3
> называет задвоение поимённо только на второй кассе. Порогом эта величина не
> служит — уровень находки задан §8, а базовая частота идёт контекстом, как и
> велит §8.1; порог между 0,05 и 0,12 был бы подгонкой под известный ответ.
>
> Отдельно вычищены 638 ложных «не локализовано» на `Максиму_касса_2`. День,
> у которого после сопоставления §5.9.2 пусты **обе** стороны — каждая проводка
> нашла выдачу, каждая выдача нашла проводку, только партнёр лежит на соседней
> дате, — падал в subset-sum с пустым списком кандидатов, гейты §7.3
> закрывались «подмножество не найдено», и день получал отказ с ненулевым
> влиянием на сальдо. Сопоставление идёт в окне `DATE_WIN` по всему периоду
> (иначе подокументная инкассация не сводится вовсе), поэтому дневная разница
> при полностью разобранном дне — это датировка, а не расхождение. Такой день
> получает статус `ОБЪЯСНЕНО_ТАЙМИНГОМ` и нулевое влияние.
>
> Две кассы остаются шумными и это признано, а не скрыто: `Максиму_касса_2`
> (инкассация `ratio` = 0,052, чуть выше порога churn) даёт 11 находок уровня
> «ОШИБКА», тогда как §11.3 говорит «дискретных ошибок
> нет»; `Кса_норма` при `ratio` 0,59–0,96 даёт 69 непроведённых выдач и одну
> «ОШИБКУ» против эталонного «без ошибок». Порог под них не двигался: 0,052
> отделено от подтверждённых эталоном 0,226 и 0,071 менее чем впятеро, и
> сдвиг порога был бы подгонкой под известный ответ.
>
> **Что ещё не сделано:** эталонные снапшоты регрессии §11.3 не заведены,
> пересчёт формул Excel (§13.6) проверен статически — рабочего офисного пакета
> в окружении нет. Расхождение по `PAYOUT_NOT_BOOKED` на Солигорске описано
> ниже и намеренно не «подгонялось».
>
> **Каталог дефектов §11.2** (`tests/synthetic/test_defect_catalog.py`): на
> каждый из семнадцати кодов §8 — генератор синтетической кассы и тест
> «внедрили → нашли ровно это», плюс три обязательных негативных кейса.
> Построение каталога само нашло три дефекта: `DUPLICATE_DOC` и
> `SERVICE_MISCLASSIFIED` стояли в таблице §8, но не выдавались ниоткуда, а
> абсолютный запрет §7.4 обходился на дне, у которого после сопоставления 1:1
> не оставалось проводок — восемьдесят чеков объявлялись «непроведёнными
> выдачами».
>
> **Проверка на реальной кассе.** Конвейер прогнан на `PAX_119011650.xlsx`
> (Солигорск, 82 611 строк, 4,3 МБ) — все эталоны §11.3 воспроизведены:
>
> | Показатель | Получено | Эталон |
> | --- | --- | --- |
> | Сальдо на конец | −1 224,80 | −1 224,80 |
> | Служебные РКО | 11 610,00 | 11 610,00 |
> | Дисбаланс логов | −807,91 | −807,91 |
> | Возвраты, скорректированные | +1 673,29 | +1 673,29 |
> | Инкассация: дней → остаток | 1 325 → 0 | 1 325 → 0 |
> | Сторно без перепроведения | 1 046,75 (строки 7991/9106) | R7991←R9106, 1 046,75 |
> | Пол кассы (04.06 и 10.06.2026) | −1 224,80 в обе даты | −1 224,80 |
> | Окно смещения 06.09–19.12.2025 | логи 0,00, 1С −2 911,51 | логи 0,00, 1С −2 911,51 |
> | Раскладка §5.10 | `unresolved` = 0,00 | \|x\| < 2,00 |
>
> V1–V3 проходят, полный прогон с тремя отчётами — 13,5 с при нормативе §12
> < 60 с. Повторный прогон даёт побитово идентичный JSON, кроме
> `meta.run_timestamp` (§13.5).
>
> **Четыре завышенных РКО §5.8.5 воспроизведены точно:** 25.10 (1 001,00),
> 06.11 (478,10), 17.11 (1 148,88) и 06.12.2025 (2 075,31) — сумма 4 703,29 при
> эталоне 4 703,29.
>
> **Открытое расхождение по `PAYOUT_NOT_BOOKED`.** Прогон даёт 24 выдачи на
> 4 681,12 против эталона §14 «35 на 8 411,73». Разбор ниже относится к
> состоянию до правок сторно и §7.4, которые убрали из счёта восемь выдач
> сторнированного дня и один приходный день; выводы о достижимости эталона он
> не меняет.
>
> Счёт 35 достижим и проверен перебором: полная свёртка §5.7 плюс
> сопоставление 1:1 §5.9.2 плюс гейт уникальности дают ровно 35 выдач. До 35
> не хватает двух дней — 16.09.2022 (209,00) и 25.11.2025 (505,77), — которые
> закрывает гейт 4 §7.3 с `p = 0,0280` и `p = 0,0110` при пороге `α = 0,01`.
> Ослабление порога до 0,05 дало бы ровно 35, и именно поэтому оно не сделано:
> подгонка порога доказательности под известный ответ — это то, против чего
> написан §7.
>
> Сумма 8 411,73 не воспроизводится ни при каких настройках. Проверены:
> служебные РКО в ряду и вне его, свёртка выключенная / только проход 1 /
> проходы 1+2 / полная, сопоставление 1:1 и без него, гейт уникальности и без
> него, сетка по `EPS_FLOOR`, `EPS_SUM`, `DATE_WIN`, `LONG_WIN`. Максимум,
> который вообще можно приписать целым выдачам на остаточных днях, — 6 769,49:
> из превышения лога 9 272,83 остаток 2 503,34 на восьми днях не выражается
> целым числом выдач ни в какой комбинации. Ближайшее к эталону значение
> (8 951,94 при 47 выдачах) даёт вариант «проходы 1+2 без переноса даты».
>
> **Ретро-сессия 01.10.2023.** На эту дату приходятся две разные группы
> проводок. Две обычные, с временем 0:00:00 и номерами 00273454/00273455,
> лежащими в октябрьском диапазоне нумерации. И пять с временем **23:59:59** и
> номерами 00289603–00290339 — эти номера принадлежат началу ноября 2023, то
> есть документы созданы месяцем позже и датированы задним числом. В той же
> сессии стоит сторно «Корректировка записей регистров 999» на −1 046,75.
>
> Содержательно это разбор апреля–июня 2023: сторнируется РКО 00231245 от
> 29.06.2023 и проводятся выдачи 09.04 (38,27), 19.04 (329,26) и 27.06 (57,66),
> задержка 96–175 дней. Отсюда два дефекта, оба исправлены:
>
> * **сторнированная проводка считалась отсутствующей.** §5.5 снимает обе
>   строки пары, день 29.06.2023 оставался с восемью выдачами и без проводок, и
>   1 046,75 попадали в отчёт дважды — как `REVERSAL_WITHOUT_REBOOK` со
>   влиянием на сальдо и как восемь «непроведённых выдач» с нулевым. §4.3
>   требует от `PAYOUT_NOT_BOOKED` буквально «этих проводок в 1С нет вовсе»,
>   поэтому такой день теперь получает статус `ОБЪЯСНЕНО_СТОРНО`;
> * **задержка проведения не показывалась.** Свёртка §5.7.2 пару не сводит:
>   перенос даты ограничен `LONG_WIN` = 31 днём, а здесь месяцы. Окно не
>   расширено — §5.7.2 держит его узким намеренно, точное совпадение сумм на
>   большом горизонте начинает сводить случайные операции. Вместо этого
>   появилась находка `POSTING_DELAY` §8, которая связывает обе строки и прямо
>   говорит, что складывать их нельзя.
>
> `PAYOUT_NOT_BOOKED` после этого — 20 дней, 24 выдачи, 4 681,12.
>
> Правило на нумерации построить не удалось, и это записано в `CLAUDE.md` как
> проверенный тупик: документы одной ретро-сессии идут подряд и маскируют друг
> друга (сосед по номеру у back-dated документа — такой же back-dated), а ряд
> ПКО этой кассы вообще не монотонен — 2022 и 2023 годы делят один диапазон
> номеров и дают 95 инверсий. Признак ищется по суммам.
>
> **Что осталось неразобранным: 13 дней на 3 544,96.** Каждый разобран до
> причины отказа, и ни одна из них не является пропуском:
>
> | Причина | Дней | Сумма |
> | --- | --- | --- |
> | гейт 2 §7.3 — сумма встречается в логе дважды | 3 | 326,85 |
> | гейт 4 §7.3 — перестановочный тест | 2 | 714,77 |
> | расхождение не выражается целыми выдачами дня | 8 | 2 503,34 |
>
> Первая группа (23.07.2022 · 18,48; 11.10.2022 · 237,97; 23.08.2025 · 70,40) —
> сумма расхождения совпадает с выдачей, но таких выдач в дне две. Что одна из
> них не проведена, установлено; какая именно — нет, и §7.3 требует отказа.
>
> Вторая (16.09.2022 · 209,00, `p = 0,0280`; 25.11.2025 · 505,77, `p = 0,0110`)
> — точное подмножество есть и единственно, но перестановочный тест не
> отвергает случайность при `α = 0,01`. Порог не ослаблен.
>
> Третья — восемь дней, где превышение лога не складывается ни из какого набора
> выдач этого дня: 1С провела часть выдачи, а не выдачу целиком. Показательный
> случай — 01.05.2024: одна проводка 146,49 против одной выдачи 292,98, ровно
> вдвое больше. §8 такого кода не содержит (`RKO_OVERSTATED` есть, обратного
> нет), и придумывать его под один день не стоит.
>
> Отдельно стоит 14.07.2025 (375,22): выдача 386,50 этого дня по сумме совпадает
> с проводкой 386,50 от 04.07.2025 — десять дней разницы. Ни свёртка §5.7.2 (её
> проход 3 требует, чтобы дневные разницы гасились точно, а тут +386,50 против
> −375,22), ни сопоставление §5.9.2 с окном `DATE_WIN` = 3 дня эту пару не
> видят. Расширять сопоставление на период я не стал: тот же поиск по всему
> ряду выдаёт и пару «999,00 через 571 день», то есть ровно то ложное сведение,
> от которого §5.7.2 предостерегает.
>
> На `PAX_119049688.xlsx` (Витебск) воспроизведены все эталоны §11.3 целиком:
> сальдо −160,89; приход — 0 дней расхождения; инкассация 69 дней → остаток 0;
> всё отклонение сведено к одной находке — строка 987, РКО 00000257506
> (в 1С 289,31, в опер-логе за день одна выдача 128,42), правилом единственной
> проводки §5.9.3 без обращения к гейтам. Сигнатура повтора §8.1 воспроизведена
> дословно: четыре РКО по 164,99 за 22 секунды (13:26:55 → 13:27:17) при
> базовой частоте «15 случаев в ПКО против 1 в РКО».
>
> Остальные шесть калибровочных касс §11.3 ещё не прогонялись — файлов в
> репозитории нет (персональные данные получателей).

## Что инструмент делает

1. Проверяет полноту и корректность разбора данных (жёсткие контроли V1–V3).
2. Сверяет бухгалтерский учёт с операционными логами по категориям потоков.
3. Отделяет **мнимые** расхождения — тайминг проводок, агрегация выходных,
   служебные операции, срез периода — от реальных.
4. Находит конкретные документы, из-за которых сальдо ушло от целевого.
5. Раскладывает отклонение по причинам без остатка.
6. Выявляет сигнатуры риска, которые сверка принципиально не ловит.
7. Формирует отчёт для службы безопасности и руководства.

## Чего он не делает

**Не квалифицирует находки как хищение или мошенничество.** Умысел не выводится
из данных. Инструмент выдаёт локализованные расхождения и «красные флаги»;
квалификация — за человеком. В отчётах используются слова «расхождение»,
«требует проверки», «сигнатура», но не «фрод», «кража», «виновен».

Не заменяет инвентаризацию кассы и не подтверждает физический остаток. Не
работает ниже дневного агрегата продаж без Z-отчётов.

Не может сказать, **какая из двух систем права** — 1С или опер-лог. Эта развилка
обязана присутствовать в блоке «что проверить» каждого отчёта.

## Три принципа

Нарушение любого считается дефектом, а не стилистическим замечанием.

1. **Детерминированность.** Один и тот же вход при одной версии правил даёт
   побитово идентичный выход. Никаких сортировок без полного явного ключа,
   никакого `random` без seed из конфига.
2. **Объяснимость.** Каждый вывод сопровождается трассировкой: какое правило
   сработало, на каких строках исходного файла, какой остаток и чем погашен.
   Инструмент, который говорит «расхождение 2 579,00», но не может показать
   почему — не выполняет задачу.
3. **Отказ вместо догадки.** При недостаточной доказательной базе возвращается
   статус «не локализовано» с причиной. Правдоподобная, но недоказанная улика —
   худший из возможных исходов: она уйдёт к живому кассиру.

## Конвейер из 12 стадий (§5 ТЗ)

```
ingest → normalize → validate → classify → neutralize_reversals →
reconcile → collapse_timing → balance_trace → localize →
decompose → rank → report
```

Каждая стадия — чистая функция с явными входом и выходом. Стадии не мутируют
вход, промежуточный результат сериализуем.

| # | Стадия | Модуль | § | Назначение |
| --- | --- | --- | --- | --- |
| 1 | `ingest` | `ingest.py` | 5.1 | Чтение `.xlsx`, SHA-256 файла, локация блоков опер-лога **по заголовкам** (позиция плавает — четыре наблюдавшихся варианта), разбор шапки |
| 2 | `normalize` | `normalize.py` | 5.2 | Наследование даты вниз, нормализация `\xa0` и форматов чисел, определение направления проводки (корр. счёт из **G** для дебета, из **E** для кредита) |
| 3 | `validate` | `validate.py` | 5.3 | V1–V3 — жёсткие инварианты, нарушение = остановка. V4–V6 — предупреждения |
| 4 | `classify` | `classify.py` | 5.4 | Категория по корр. счёту; приход = только продажи + авансы. Классификация опер-лога, включая верифицируемый time-fallback |
| 5 | `neutralize_reversals` | `reversals.py` | 5.5 | Гашение сторно-пар + проверка «была ли фактическая выдача»: снятая и не перепроведённая проводка — потеря учёта, а не нейтральная пара |
| 6 | `reconcile` | `reconcile.py` | 5.6 | Нетто, брутто, ratio по трём категориям; возвраты в вариантах «как есть» и «скорректировано» |
| 7 | `collapse_timing` | `timing.py` | 5.7 | Детектор лага + свёртка в три прохода + календарная агрегация выходных. Без неё инструмент показывает сотни ложных строк |
| 8 | `balance_trace` | `balance.py` | 5.8 | Нарастающее сальдо, инвариант неотрицательности, «пол кассы» с фильтром полноты выемки, датирование смещения PELT/CUSUM/EWMA |
| 9 | `localize` | `localize.py` | 5.9 | Сопоставление 1:1, правило единственной проводки, subset-sum **с четырьмя гейтами** §7 |
| 10 | `decompose` | `decompose.py` | 5.10 | Раскладка без остатка и её перевод в причинную форму с документами |
| 11 | `rank` | `rank.py` | 5.11 | Материальность, confidence, ранжирование, свёртка тривиального |
| 12 | `report` | `report/` | 5.12, 9 | Excel (основной), JSON (эталоны регрессии), HTML |

Вне конвейера: `signatures.py` (§8.1–8.2) — повторы, разрывы нумерации,
дубликаты, документы вне рабочих часов. Это то, что сверка не ловит по
построению.

## Команды CLI (§10 ТЗ)

```bash
cashforensics analyze <файл.xlsx> [--config CONF] [--target 0.00]
                                  [--out DIR] [--format excel,json,html]
cashforensics batch <директория> [--summary сводка.xlsx]
cashforensics verify <файл.xlsx>                     # только валидация §5.3
cashforensics explain <файл.xlsx> --finding <код>    # трассировка одной находки
```

Пакетный режим обязателен: сеть ПВЗ — десятки касс, нужна сводная таблица с
ранжированием по величине отклонения и числу находок (peer comparison).

Флаги обратимости (§12): `--no-collapse` и `--no-reversal-neutralization`
отключают любое автогашение и показывают сырую картину. `--audit-log` добавляет
лист журнала: что погашено, чем, каким правилом.

## Установка и разработка

Требуется Python 3.11+ и [uv](https://docs.astral.sh/uv/).

```bash
uv sync                      # окружение из uv.lock
uv run pytest                # все тесты
uv run pytest -m unit        # только юниты (§11.1)
uv run pytest -m synthetic   # синтетические кассы (§11.2)
uv run ruff check .          # линт
uv run ruff format .         # форматирование
uv run mypy                  # строгая типизация
```

Маркеры pytest соответствуют трём уровням §11: `unit`, `synthetic`,
`regression`.

Проверить разбор конкретной выгрузки:

```bash
uv run cashforensics verify путь/к/выгрузке.xlsx
```

Команда печатает найденную раскладку блоков, период, контрольные суммы и
результат каждого контроля V1–V6, а строки, которые не удалось отнести к
кассе, выводит списком с номерами — «тихих» пропусков нет (§12). Код возврата
1 означает нарушенный жёсткий инвариант: разбор неполон, и дальнейший анализ
дал бы правдоподобный, но неверный результат.

## Структура

```
config/
  default.yaml           # все пороги §6, версионируется вместе с кодом
  holidays_by.yaml       # праздники РБ для календарной агрегации §5.7.3
src/cashforensics/
  models.py              # модель данных §4 + схема конфига §6
  ingest.py … rank.py    # стадии конвейера §5
  signatures.py          # сигнатуры риска §8
  report/                # excel.py, json_out.py, html.py — §9
  cli.py                 # команды §10
tests/
  test_env.py            # smoke-тест окружения
  unit/                  # §11.1
  synthetic/             # §11.2 — внедрили дефект → нашли ровно его
  regression/fixtures/   # §11.3 — восемь калибровочных касс
```

## Ключевые ограничения (§16 ТЗ)

Их обязан отражать каждый отчёт:

* Инструмент не может сказать, какая из двух систем права. Если суммы в
  проблемных ордерах подтвердятся подписями клиентов, ошибка окажется в выгрузке
  фронтальной системы, а не в 1С.
* Дисбаланс самих логов измеряется, но не объясняется: его причина лежит вне
  обоих файлов.
* Сигнатуры повторов **не являются уликой** — базовая частота повторов в этих
  данных высока.
* Датировка смещения даётся **окном**; точная дата из ряда сальдо не выводима.
* При отсутствии блока ПКО приход не верифицируется вовсе, и раскладка честно
  помечается «частично».
